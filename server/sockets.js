// join room
const joinRoom = (socket, data) => {
  console.log(socket, data);
};

// send message
const sendMessage = () => {};

// typing indicator

// stop typing indicator

// countdown

// leave room
